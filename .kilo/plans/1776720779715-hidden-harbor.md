# Plan to change CustomNode layout to two columns

This plan outlines the steps to change the layout of `CustomNode.tsx` so that Inputs and Outputs are displayed in two columns (left and right) instead of being stacked vertically.

## Analysis of current implementation

In `src/components/nodes/CustomNode.tsx`:
- The main container for both Inputs and Outputs is at line 9: `<div className="flex flex-col gap-2">`.
- The Inputs container is at line 11: `<div className="flex flex-col gap-2">`.
- The Outputs container is at line 26: `<div className="flex flex-col gap-2">`.
- The outer div has a minimum width of `min-w-[150px]` at line 6.

## Proposed Changes

1.  **Update the container of Inputs and Outputs**:
    - Change the `flex-col` class to `flex-row` at line 9 to allow them to sit side-by-side.
    - Increase the `gap` between the two columns for better visual separation (e.g., `gap-4`).

2.  **Adjust the minimum width of the node**:
    - Increase the `min-w-[150px]` at line 6 to something larger, like `min-w-[250px]`, to ensure there is enough horizontal space for both columns.

## Implementation Steps

1.  Modify `src/components/nodes/CustomNode.tsx`:
    - Update line 6 to increase `min-w`.
    - Update line 9 to change `flex-col` to `flex-row` and adjust the `gap`.
