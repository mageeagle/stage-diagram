# Plan: Add Title and Prepared By Inputs to Export PDF

## Overview
Add two inputs (Title and Prepared by) to the Node List Modal, and display them in the exported PDF below the title.

---

## Step 1: Update Type Definitions
**File:** `src/components/diagram/node-list-modal-types.ts`
- Add `title: string` and `preparedBy: string` to `NodeListModalProps` interface
- Default values will be provided in the modal component

## Step 2: Update NodeListModal Component
**File:** `src/components/diagram/node-list-modal/NodeListModal.tsx`
- Add `useState` hooks for `title` (default: "Technical Rider") and `preparedBy` (empty string)
- Add two input fields in the modal body (below "Show details" checkbox)
- Pass `title` and `preparedBy` props to `NodeListHeader`

## Step 3: Update NodeListHeader Component
**File:** `src/components/diagram/node-list-modal/NodeListHeader.tsx`
- Accept `title` and `preparedBy` props
- Render title and prepared by info below the current "Existing Nodes" header
- Include current date in YYYY.MM.DD format

## Step 4: Update Store (Optional - for persistence)
**File:** `src/store/useStore.ts`
- Add `title: string` and `preparedBy: string` to store state
- Add setters if needed for undo/redo support
- *Decision pending: Store these values or keep them local-only?*

## Step 5: Export Flow (No changes needed)
**Files:**
- `src/components/diagram/node-list-modal/NodeListModal.tsx` - already calls `exportToPdf(report)`
- `src/components/diagram/node-list-modal/pdf-export-utils.ts` - currently doesn't accept modal data
- `src/components/diagram/ExportButton.tsx` - PDF handler

**Strategy:** The PDF export currently uses `toSvg()` + `window.print()` approach, which captures the entire DOM. Since we're adding the title/preparedBy to the modal UI (`NodeListHeader`), the PDF will automatically capture it when printed.

---

## Summary of Changes

| File | Action | Reason |
|------|--------|--------|
| `node-list-modal-types.ts` | Add props | Define interface for new inputs |
| `NodeListModal.tsx` | Add state & UI | Store and render new input fields |
| `NodeListHeader.tsx` | Add props & UI | Display title, preparedBy, date in PDF |
| `useStore.ts` | Decision needed | Determine if values need persistence |

---

## Implementation Approach
- The simplest approach: Keep values in modal state (local to modal lifecycle)
- PDF will auto-capture via DOM printing (current `toSvg`+`print` flow)
- No need to modify `pdf-export-utils.ts` or `ExportButton.tsx`

---

## Questions for Confirmation
1. **Store persistence**: Should `title` and `preparedBy` be stored in `useStore` for undo/redo and persistence across modal reopen?
   - **Option A**: Local state only (simpler, no persistence)
   - **Option B**: Store in store (full undo/redo support)
2. **Input placement**: Where should the two input fields appear in the modal?
   - **Option A**: Below "Show details" checkbox
   - **Option B**: In a separate section with label "Export Settings"

---

## Next Steps
After user confirmation:
1. Update type definitions
2. Update NodeListModal with inputs
3. Update NodeListHeader with display
4. Decide on store persistence implementation
