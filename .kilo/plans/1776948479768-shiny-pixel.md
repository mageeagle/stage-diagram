# Plan: Add Cables Section to NodeListModal

## Goal
Add a "Cables" section to the end of the `NodeListModal` component that displays the count of edges for each cable type defined in the store.

## Implementation Steps

1.  **Modify `src/components/diagram/NodeListModal.tsx`**:
    *   Destructure `edges` and `cableTypes` from `useStore`.
    *   Calculate the counts for each cable type by filtering the `edges` array.
    *   Add a new UI section for "Cables" after the `NodeListContent` component.
    *   The section will include:
        *   A header with the text "Cables", styled consistently with other headers in the modal (using Tailwind classes found in `NodeListContent`).
        *   A list of cable types and their respective counts, using `flex justify-between` for layout.

## Verification
*   Check if the "Cables" section appears at the bottom of the `NodeListModal`.
*   Verify that the counts correctly reflect the number of edges of each type in the current diagram.
*   Ensure the styling is consistent with the rest of the application.
