# Plan: Group templates by type in PropertyInspector

## Goal
Group templates by their `type` in the `PropertyInspector` component when no node is selected.

## Implementation Steps

1.  **Modify `PropertyInspector.tsx`**
    *   Locate the template rendering logic (lines 53-89).
    *   Implement grouping logic for `templates` based on their `type` property.
    *   Update the UI to display:
        *   A heading for each template type.
        *   The templates belonging to that type underneath the heading.
    *   Ensure styling is consistent with the existing UI.

## Details

-   I will use `Object.entries` on a grouped object (created using `reduce`) to iterate over types and their respective templates.
-   For each type, I'll render a small, styled heading (e.g., using `text-xs font-bold text-gray-500 uppercase`).
-   The templates themselves will remain in their current list-item format.

## Verification
-   Manually verify that templates are grouped correctly by type in the UI.
-   Check that all templates are still visible and their actions (apply, edit, delete) still work.
