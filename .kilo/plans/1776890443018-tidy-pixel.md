# Goal: Refactor NodeListModal to use a structured table view for listing nodes.

## Overview
The existing list display in the NodeListModal component needs to be replaced with a summary table format. This table must have two columns: a larger column for the node's name and details, and a smaller column for the total quantity of nodes. The aggregation rules depend on the selected tab ("None", "Types", or "Location").

## Step 1: Code Analysis (Read nodes/NodeListModal.tsx)
Action: Read the content of `src/components/nodes/NodeListModal.tsx` to understand the current rendering and data structure.
Goal: Identify where the node list is rendered and what data processing functions exist.

## Step 2: Define Data Aggregation Logic (Internal/Helper Functions)
Action: Develop helper functions (or modify existing ones) to process the source array of nodes based on the active tab:
1.  **Tab "Types" & "None" Logic:**
    *   Group nodes by: (Name, Type).
    *   For each unique group, calculate the total quantity of nodes belonging to that (Name, Type) pair.
    *   The row displayed should show the Name (and possibly the Type as part of the "other info") and the total quantity. Location information must be suppressed.
2.  **Tab "Location" Logic:**
    *   Group nodes solely by Location.
    *   For each unique location, calculate the total quantity of nodes at that location.
    *   The row displayed should show the Location and the total quantity.

## Step 3: Component Refactoring (Update NodeListModal.tsx)
Action: Refactor the rendering logic inside `NodeListModal.tsx`:
1.  Introduce the table component structure (e.g., using semantic HTML or a specific UI library component).
2.  Implement a tab switch/handler logic to conditionally render the appropriate aggregated and formatted table based on the active tab state.
3.  Integrate the aggregation results from Step 2 into the table structure.

## Step 4: Implementation Details (Styling and Types)
Action: Review and update any necessary interfaces or component props used in `NodeListModal.tsx` to accommodate the aggregated data structure and the new table view.
Goal: Ensure the display remains visually consistent with existing component styles while implementing the two-column layout.

## Confirmation
I will execute these steps and provide a complete plan implementation. I will confirm the functional change after implementing the class/functionality logic first, and then review/test the component render.

-- Planning complete. Ready for the user to confirm the plan.